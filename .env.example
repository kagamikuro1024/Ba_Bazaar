POSTGRES_USER=ba_bazaar
POSTGRES_PASSWORD=change_me
POSTGRES_DB=ba_bazaar
DATABASE_URL=postgresql://ba_bazaar:change_me@localhost:5432/ba_bazaar?schema=public

API_PORT=3000
WEB_PORT=5173
VITE_API_BASE_URL=http://localhost:3000

# Future/internal use for mock JWT-compatible auth. Replace before real auth.
JWT_SECRET=replace_with_a_long_random_secret
JWT_ACCESS_TTL=8h
JWT_REFRESH_TTL_DAYS=30
ALLOW_MOCK_AUTH=false
VITE_SHOW_DEMO_ACCOUNTS=false

# AI Assistant (Bedrock OpenAI-compatible mode).
# Set BEDROCK_API_KEY to enable the real LLM. Leave unset for the
# deterministic stub provider (dev / CI / offline).
# BEDROCK_API_KEY=***
# BEDROCK_MODEL=openai.gpt-oss-120b-1:0
# BEDROCK_BASE_URL=https://bedrock-runtime.us-east-1.amazonaws.com/openai/v1
